<!DOCTYPE html>
<html lang="en">
	<%= head title: "#{doc_title(@entry)} / #{home(@preload).props["name"]}" %>
	<body class="h-entry">
		<%= header preload: @preload, cur_url: @entry.url, feed_urls: @feed_urls %>
		<main>
			<article>
				<%= entry preload: @preload, cur_url: @entry.url, entry: @entry, feed_urls: @feed_urls %>
			</article>
		</main>
		<%= footer [] %>
	</body>
</html>
