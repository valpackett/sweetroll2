<!DOCTYPE html>
<html lang="en">
	<%= head title: "#{doc_title(@entry)} / #{home(@preload).props["name"]}" %>
	<body class="h-entry">
		<%= header preload: @preload, cur_url: @entry.url %>
		<main>
			<article>
				<%= entry preload: @preload, cur_url: @entry.url, entry: @entry %>
			</article>
		</main>
		<%= footer [] %>
	</body>
</html>
